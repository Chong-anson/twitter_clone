<style>
  .tweet-compose{
    display:flex; 
    flex-direction: column; 
    align-items: center;
  }
  .tweet-content{
    min-width: 200px;
    min-height: 100px;
    margin-left: 5px;
    align-items: center; 
  }
</style>

<h2>New Tweet!</h2>
<form action="/tweets" class="tweet-compose" method="POST" data-tweets-ul="#feed">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">
    <textarea 
      class="tweet-content" 
      name="tweet[content]"
      placeholder="Content"></textarea>
    <strong class="chars-left">140 characters remaining</strong>
  <ul class="mentioned-users"></ul>
  <br>
  <button class="tweet-add-mention">Add Mention</button>
  <input type="Submit" value="Post Tweet!">
</form>
